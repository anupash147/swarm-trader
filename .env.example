# AI Hedge Fund — Environment Variables
# Copy this to .env and fill in your keys.
# .env is gitignored and will NOT be committed.

# Alpaca Paper Trading — Primary / Swing account (required)
ALPACA_API_KEY=
ALPACA_API_SECRET=
# Optional: account ID for logging (found in Alpaca dashboard)
# ALPACA_ACCOUNT_ID=

# Alpaca Day Trading account (optional — separate account for intraday trades)
# If not set, both modes share the primary account.
ALPACA_DAY_API_KEY=
ALPACA_DAY_API_SECRET=
# ALPACA_DAY_ACCOUNT_ID=

# Financial Modeling Prep (optional, recommended — 250 free req/day)
# Sign up at https://financialmodelingprep.com/ for a free API key.
# If set, used as primary source for financial metrics (P/E, margins, etc.)
# If not set, falls back to yfinance (unreliable rate limits).
FMP_API_KEY=

# Financial data (DEPRECATED — no longer required)
# api.py now uses free sources: FMP / yfinance + SEC EDGAR XBRL.
# The old financialdatasets.ai implementation is backed up as api_original.py.
# FINANCIAL_DATASETS_API_KEY=

# SEC EDGAR requires a User-Agent header (name + email). Optional override:
# SEC_USER_AGENT=YourApp admin@example.com

# LLM providers (only needed if NOT using Ollama)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=
DEEPSEEK_API_KEY=
GOOGLE_API_KEY=

# A2A Peer Signal Sharing (optional — intel_exchange.py)
# Configure if you run two swarm-trader instances and want them to share signals.
# Generate tokens with: openssl rand -base64 24 | tr -d '=+/' | cut -c1-32
SWARM_AGENT_NAME=my-agent
PEER_A2A_URL=http://peer-host:9092/a2a/tasks/send
PEER_A2A_TOKEN=
